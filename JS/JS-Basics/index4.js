//Datatype

// Primitives/Value Types
let name='Dheeraj'		//String literal	//string can be in double and single qotes
let age=23;				//Number literal	// there is no 'int','float' datatypess in JS, There is only 'number'
let isApproved = true;	//Boolean litral
let firstName = undefined;		//undefined
let selectedColor = null;	//null  //is for object

console.log(name,age,isApproved,firstName,selectedColor);

//Dynamic Typing :-
	//write below code on the console
		/*	typeof name;
			name=2;
			typeof name;
		*/
	
	//the Datatype is changed of "name" variable