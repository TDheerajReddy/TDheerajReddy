//Variable
let name='Dheeraj', address,state="Banglore";					//'let' OR 'var' for declaring variables //default value is "undefined"
console.log(name,address,state);
//string can be in double and single qotes

let interstRate=0.3;
interstRate=1;
console.log(interstRate);

//Constant variable
const profitRate=0.5;
//profitRate=5;			//gives error, because we can not re-assign the constant variable
console.log(profitRate);